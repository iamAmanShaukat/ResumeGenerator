from reportlab.lib import colors

class Colors:
    PRIMARY = colors.Color(0.2, 0.3, 0.5)  # Professional blue
    SECONDARY = colors.Color(0.4, 0.4, 0.4)  # Dark gray
    ACCENT = colors.Color(0.1, 0.4, 0.6)  # Accent blue
    LIGHT_GRAY = colors.Color(0.9, 0.9, 0.9)  # Light gray
    BLACK = colors.black
    WHITE = colors.white